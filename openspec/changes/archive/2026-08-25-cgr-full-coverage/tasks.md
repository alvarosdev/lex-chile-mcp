## 1. Contrato y cliente genérico

- [ ] 1.2 Refactor `internal/cgr/cgr_client.go`: `SearchParams` con `Source`, `validSource()` whitelist exacta `TrimSpace+ToLower` + rechazo `/ . %` y `len>20` **antes** de `Post("/"+source)`, `validQuery()` max 500 trunc+aviso, `validPage()` max 500, `ContentLength>6 MB` rechazo antes de `Unmarshal`, filtro `old_url`/`172.30.x` nunca expuesto, `hit._index` check contra `cgr-{source}`, y `retryConditions` con `429` + `Retry-After` + `breaker 3` para pesados; añadir wire types `cgrInstructivoSource`/`cgrContableSource`/`cgrAuditoriaSource`/`cgrConsolidadoSource`/`cgrCuentaSource`/`cgrLegislacionSource` y caches por familia (`cacheMax` light 100, heavy 20) con TTL; logger trunca `query` a 80 chars, nunca loguea documento
- [ ] 1.3 Ampliar `internal/cgr/sanitize.go` + `garbage.go`: `SanitizeAuditoria`/`SanitizeContable`/`SanitizeLegislacion`/`SanitizeConsolidado`/`SanitizeCuenta` con `html.UnescapeString` + strip tags `<[^>]+>` + `normalize()` para **todos** los campos, `texto_raw`/`_raw` nunca expuesto, tests con fixtures HAR reales (`Quillota`, `municipalidad`, `toma de razón`) + `TestSanitize_StripsTags`/`TestNoInternalURLLeak`

## 2. Tools search y gets

- [ ] 2.1 Implementar `internal/tools/search_cgr.go`: `SearchCgrArgs{query, exact_search, order, page, source}` con validación estricta `source`/`order`/`page` (sin I/O si inválido), `validQuery` trunc 500, `page<=500`, `RegisterSearchCgr`, `buildSearchOutput` por source con filtro `old_url`, `format` LLM-first con aviso cap `gte 10000` y URLs por tipo; alias delgado `search_cgr_dictamenes.go` que fija `source=dictamenes`; tests `TestSearch_InvalidSource_NoNetwork`/`TestQueryTruncated`/`TestPageOverflow`/`TestResponseTooLarge`
- [ ] 2.2 Implementar `internal/tools/get_cgr_instructivo.go` y `get_cgr_contable.go`: validadores `len<=40` + `TrimSpace+ToUpper` + regex ancladas `instructivo ^[A-Z]{0,3}[0-9]{1,6}N[0-9]{2}$` / `contable ^(E[0-9]{1,6}|OFE[0-9]{10,13})$`, rechazo `/ . %` y control chars sin I/O, `hit._index` check, `RegisterGet...`, `format` con `char_count` y `structuredContent` tipado, error “no encontrado” si `total==0` o cross-index
- [ ] 2.3 Implementar `internal/tools/get_cgr_auditoria.go` y `get_cgr_consolidado.go`: `auditoria_id ^([0-9]{1,4}/[0-9]{4}|[A-Z]*[0-9]+N[0-9]{2})$` / `consolidado_id ^CIC[0-9]{1,4}/[0-9]{4}$` con `len<=40`, truncado `contenido_pdf` 30k + aviso + `pdf` link, `resena`+`contenido_extraido` sanitizados, `hit._index` check; tests `TestID_Validation_LenAndRegex`/`TestHitIndex_Mismatch_NotFound`/`Test429_RetryAfter`
- [ ] 2.4 Implementar `internal/tools/get_cgr_cuenta.go` y `get_cgr_legislacion.go`: `cuenta_id ^[0-9]{1,6}$` y `legislacion_id ^(RZA|FRA)[0-9]{9,12}$` con `len<=40`, `pdf`/`pdf2` y `texto_` sanitizados, `hit._index` check, `ContentLength` checks
## 3. Wiring y validación

- [ ] 3.1 Actualizar `internal/tools/tools.go` y `cmd/lex-chile-mcp/main.go` para registrar `search_cgr` + 6 `get_*` + mantener `count_cgr_jurisprudencia` y `search_cgr_dictamenes` alias; un solo `CgrClient` inyectado por constructor; verificar `api.resources.yaml` con `cgr_search` base y `cgr_count` `/count/todos` validado `fail-fast` en `LoadEmbedded()`
- [ ] 3.2 Añadir fixtures `internal/cgr/testdata/search_instructivos_municipal.json`, `search_contable_municipalidad.json`, `search_consolidado_licencias.json`, `search_legislacion_toma.json`, `search_auditoria_licencias.json`, `search_cuentas_municipalidad.json` (1 JSON por source capturado live) y tests `httptest` para `Search` por source, `Get*` por ID, `Count` con path `/count/todos` y buckets 8, `TestNoInternalURLLeak`, `TestHitIndex_Mismatch`
- [ ] 3.3 Ejecutar `make check` (build+vet+test), `make fmt-check`, `openspec validate --strict`; verificar `tools/list` incluye 14 tools y `search_cgr` con `source` enum `len 9`, y que `count` bajo timeout sugiere `search` directo (no bloquea); verificar `retry 429` con `httptest` 429+Retry-After y breaker `5/2/30` vs `3` para pesados
## 4. Prompts CGR sin saturar (camino B) + README

- [ ] 4.1 Actualizar `internal/prompts/cgr/prompts.go`: añadir consts `toolSearchCgr`/`toolGetContable`/`toolGetInstructivo`/`toolGetAuditoria`/`toolGetConsolidado`/`toolGetCuenta`/`toolGetLegislacion`, `ToolNames()` 3→10, `expectedPromptNames` 4→6, `allowedPlaceholders` con `source`/`order`/`exact_search`/`contable_id`/`instructivo_id`/`auditoria_id`/`consolidado_id`/`cuenta_id`/`legislacion_id`/`lang`, mantener `//go:embed prompts.yaml` y `LoadEmbedded()` con `missingkey=error`; escape `{{.query}}` entre comillas y test `TestPromptInjection_QueryWithBraces` + `TestNoLogPII`
- [ ] 4.2 Reescribir `internal/prompts/cgr/prompts.yaml`: `search_jurisprudence` genérico con tabla `source` (dictamenes, instructivos IN23, contable NICSP, auditoria 10k, consolidados CIC21, cuentas, legislacion AFECTO), flujo `count?(opcional)` → `search_cgr(source,order)` → `get_*`, hints `order=score si total>500` y pacing 3-4s, disclaimer + `{{if .lang}}`; añadir `analyze_contable_instructivo` y `analyze_auditoria_consolidado` con `url`/`pdf_url`/`documento_cic_pdf_web`, hedge y preamble corto ref a `interpret_dictamen`; interpolación `query="{{.query}}"` escapada
- [ ] 4.3 Actualizar `internal/prompts/cgr/prompts_test.go`: 6 prompts, `ToolNames()` 10, `allowedPlaceholders` con `source`, `TestTemplatesReferenceOnlyRegisteredTools` por dominio, `mock.Anything` para ctx, y `TestLangPlaceholder` para `{{.lang}}`
- [ ] 4.4 Verificar `prompts/list` 16 (10 BCN + 6 CGR), `ToolNames()` CGR 10, `allowedPlaceholders` con `source`, y que `search_jurisprudence` sin `source` default dictamenes sigue funcionando; `README.md` ya cubierto en 3.4 (14→16 guías, tabla prompts por dominio, **Qué puedes preguntar** +4 ejemplos `IN23`/`E080961`/`CIC21`/`AFECTO`)
